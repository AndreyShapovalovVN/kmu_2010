#!/usr/bin/env python
from setuptools import setup, find_packages

setup(
    name='UkrTranslit',
    version='0.0.1',
    description='Transliteration cyrilic to latn',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)